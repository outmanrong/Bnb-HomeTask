import * as React from "react";

export default function SmallContent({ color }) {
  return (
    <div className={"small-content"} style={{ backgroundColor: color }}>
      SmallContent
    </div>
  );
}
